<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinApp"
             x:Class="XamarinApplication.View.MainPage">
    <ScrollView>
    <StackLayout BindableLayout.ItemsSource="{Binding User}">
            <BindableLayout.ItemTemplate>
            <DataTemplate>
                <Frame BorderColor="Navy" BackgroundColor="Azure" CornerRadius="10">
                    <Label TextColor="Navy">
                        <Label.Text >
                            <x:Binding></x:Binding>
                        </Label.Text>
                    </Label>
                </Frame>
            </DataTemplate>
        </BindableLayout.ItemTemplate>
    </StackLayout>
    </ScrollView>

</ContentPage>
